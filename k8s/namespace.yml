# ── Namespace ──────────────────────────────────────────────────────────────────
# Dedicated namespace to isolate all ACEest Fitness resources
apiVersion: v1
kind: Namespace
metadata:
  name: aceest
  labels:
    app: aceest-fitness
    managed-by: kubectl
